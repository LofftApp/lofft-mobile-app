import React from 'react';
import {Text, StyleSheet} from 'react-native';

const CostsScreen = () => {
  return <Text>Costs screen</Text>;
};

const styles = StyleSheet.create({});

export default CostsScreen;
