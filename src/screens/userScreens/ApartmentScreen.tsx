import React from 'react';
import {Text, StyleSheet} from 'react-native';

const ApartmentScreen = () => {
  return <Text>Apartment Screen</Text>;
};

const styles = StyleSheet.create({});

export default ApartmentScreen;
